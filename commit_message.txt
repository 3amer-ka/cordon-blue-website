🧪 Add test coverage for optimize_hotel_resort.py

🎯 **What:** The `optimize_hotel_resort.py` file contained image optimization logic utilizing `PIL` that completely lacked unit testing coverage, presenting a testing gap and reducing confidence during refactoring.
📊 **Coverage:** This PR introduces `test_optimize_hotel_resort.py` utilizing the `unittest` framework. It successfully mocks out `PIL` to allow testing in restricted environments and tests the `FileNotFoundError` handling, proper resizing of standard `RGB` images, and conversion and saving functionality of `RGBA` images.
✨ **Result:** Increased testing coverage and reliability by fully asserting correct processing paths and error handling without requiring actual external library installations in the test runner.
