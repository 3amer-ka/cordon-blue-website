🧹 [code health] Deduplicate performance measurement scripts

🎯 What:
Combined identical logic from `measure_perf_new.py` into `measure_perf.py`. Used `argparse` to allow specifying `--port` and `--image` dynamically, defaulting to previous `measure_perf.py` values.

💡 Why:
Removes duplicate code, reducing maintenance overhead. Allows flexibility without needing separate scripts for every configuration. Re-wrapped logic in `if __name__ == '__main__':` to allow server classes to be imported.

✅ Verification:
Confirmed the refactored script functions correctly via:
- `python3 measure_perf.py` defaults
- `python3 measure_perf.py --port 8001 --image /assets/images/resort-design-optimized.jpg`
Ran `python3 test.py` suite.

✨ Result:
Cleaner repository and improved robustness. Deleted `measure_perf_new.py`.
